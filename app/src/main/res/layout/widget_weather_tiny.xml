<?xml version="1.0" encoding="utf-8"?>
<!--
  The 2x1 shape: temperature with its condition emoji, city underneath. No refresh glyph - at this
  width it would crowd the temperature, and the hourly tick plus opening the app both still
  refresh. Tapping the tile opens the app, as at every size.

  Ids are shared with the larger layouts so one builder can populate any of them; the views this
  shape has no room for simply do not exist here, and the builder must not assume they do.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:gravity="center_vertical"
    android:orientation="vertical"
    android:padding="8dp">

    <!-- Shown instead of the data when there is no city or no forecast at all. -->
    <TextView
        android:id="@+id/widget_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:singleLine="true"
        android:textColor="@color/widget_text"
        android:textSize="13sp"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/widget_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/widget_emoji"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:includeFontPadding="false"
                android:textSize="16sp" />

            <TextView
                android:id="@+id/widget_temp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:includeFontPadding="false"
                android:paddingStart="5dp"
                android:textColor="@color/widget_text"
                android:textSize="20sp" />
        </LinearLayout>

        <TextView
            android:id="@+id/widget_city"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:includeFontPadding="false"
            android:singleLine="true"
            android:textColor="@color/widget_text_dim"
            android:textSize="11sp" />
    </LinearLayout>
</LinearLayout>
